package com.company;

import java.util.Arrays;

public class Demo {
    public static void main(String[] args) {
        int[] accounts = {8,1,2,2,3};
        int max=0;
        for(int i=0; i<=accounts.length;i++)
        {
            int sum=0;
           // for(int j=0;j<accounts[i].length;j++)
            {
        //        sum=sum+accounts[j];
            }

            if(sum>max)
            {
                max=sum;
            }
        }

        System.out.println(max);

    }}

